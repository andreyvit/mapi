import { testDb } from '../../../config';
import createApp from '../../app';
import { assert } from 'chai';
import mocha from 'mocha';
import sinon from 'sinon';
import news from '../../get/news';

// sinon.stub

// var app = createApp(testDb);

describe('Routes tests', function() {

});
